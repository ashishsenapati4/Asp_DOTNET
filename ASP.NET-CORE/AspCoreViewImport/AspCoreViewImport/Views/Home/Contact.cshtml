@*@model List<Student>;*@
@{
    ViewData["Title"] = "Contact";
}

<h1>Contact</h1>
@{
    foreach (var item in Model)
    {
        <h3>@item.Id</h3>
        <h3>@item.Name</h3>
        <h3>@item.Gender</h3>
        <br />
    }
}

